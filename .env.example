# Absolute path to the directory containing docker-compose.yml — no trailing slash
BASE_DIR=/home/ai-man/arr-stack

# WireGuard private key from ProtonVPN (Downloads -> WireGuard config -> "Gluetun" platform)
WG_PRIVATE_KEY=your_protonvpn_wireguard_private_key_here
